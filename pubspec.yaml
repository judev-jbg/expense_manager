name: expense_manager
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # Core básico
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # Base de datos
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.3

  # Utilidades
  uuid: ^4.2.1
  intl: ^0.18.1

flutter:
  uses-material-design: true
